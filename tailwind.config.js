// Delegate to the actual Tailwind configuration stored alongside the source
// files. This keeps tooling happy while satisfying the requested structure.
module.exports = require('./src/tailwind.config.js');

